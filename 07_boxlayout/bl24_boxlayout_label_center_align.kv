## Demo of center-aligned Label
## The only thing needed is to set the pos_hint
## property. Arguments take dictionary form.
## In this case, we set 'center_x' to '.5' so
## the Label is horizontally centered in the BoxLayout.
<MyBoxLayout>:
	orientation: "vertical"
	spacing: 20
	padding: 20
	size: root.width, root.height ## this makes no difference

	Label:
		id: my_label
		text: "Here's a label"
		font_size: 32
		size_hint: .5, 1
		pos_hint: {'center_x': .5}
		canvas.before:
			Color:
				rgba: 0, 0, 1, 1
			Rectangle:
				pos: self.pos
				size: self.size
	Button:
		id: my_button
		text: "Next Label"
		font_size: 32
		size_hint: 1, 1
		on_release: root.release()
